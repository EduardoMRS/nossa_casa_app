# Nossa Casa Native

Aplicativo iOS e Android em NativePHP Mobile 4. A interface é nativa, construída com `NativeComponent` e EDGE; o app consome os contratos canônicos `/api` do servidor Nossa Casa.

Consulte [BUILD.md](BUILD.md) para preparar o ambiente, executar testes, gerar builds de homologação e publicar nas lojas.
